#include <bits/stdc++.h>

using namespace std;

int main(){

    int a;
    string b;
    cin >> a;
    vector<int> v;

    long long cai;

    cout << fixed << setprecision(2) << "" << endl;

    for(int i=0 ; i<3 ; i++){
        int sac = i;

        cout << sac << endl;
    }

    for(int i=1 ; i<=a ; i++){
        cout << (i) << " " << (i*i) << " " << (i*i*i) << endl;
    }

    return 0;
}
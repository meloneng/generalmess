#include <bits/stdc++.h>

using namespace std;

int main(){

    int a;
    string b;
    cin >> a;
    vector<int> v;
    

    for(int i=1 ; i<=a ; i++){
        cout << (i) << " " << (i*i) << " " << (i*i*i) << endl;
    }

    return 0;
}
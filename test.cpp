#include <bits/stdc++.h>

using namespace std;

int main(){

    int a;
    string b;
    vector<int> v;

    long long cai;

    cout << fixed << setprecision(2) << "" << endl;

    for(int i=0 ; i<3 ; i++){
        int sac = i;

        cout << sac << endl;
    }

    for(int i=1 ; i<=a ; i++){
        cout << (i) << " " << (i*i) << " " << (i*i*i) << endl;
    }

    cout << 'A'+1 << "   " << 'Z'+1 << endl;

    b = "manga";

    for(char i: b){
        cout << i <<endl;
    }

    return 0;
}